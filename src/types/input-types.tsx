export interface mouseLoaderProps {
  params: {
    id: string;
  };
}

export interface keyboardLoaderProps {
  params: {
    id: string;
  };
}

export interface headsetLoaderProps {
  params: {
    id: string;
  };
}

export interface bluetoothLoaderProps {
  params: {
    id: string;
  };
}

export interface accessoryLoaderProps {
  params: {
    id: string;
  };
}
export interface IPressCenterNewsLoaderProps {
  params: {
    id: string;
  };
}
